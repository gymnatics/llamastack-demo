# LlamaStack Configuration - Workshop Phase 2
# Adds HR MCP server to the existing Weather MCP
#
# This demonstrates how to iterate a LlamaStack distribution by adding more tools.
# Users apply this config to see how easy it is to extend their AI agent's capabilities.
#
# Phase 1 (initial): vLLM + Weather MCP only (3 tools)
# Phase 2 (this):    vLLM + Weather + HR MCPs (8 tools)
#
# Usage:
#   oc apply -f manifests/workshop/llama-stack-config-workshop-phase2.yaml -n user-XX
#   oc delete pod -l app=lsd-genai-playground -n user-XX
#
apiVersion: v1
kind: ConfigMap
metadata:
  name: llama-stack-config
  # Note: namespace will be set by user when applying with -n flag
data:
  run.yaml: |
    # Workshop Phase 2: vLLM + Weather + HR + Jira MCPs
    version: "2"
    image_name: rh
    apis:
    - agents
    - datasetio
    - files
    - inference
    - safety
    - scoring
    - tool_runtime
    - vector_io
    providers:
      inference:
      - provider_id: sentence-transformers
        provider_type: inline::sentence-transformers
        config: {}
      # Single Provider: vLLM (local) - same as Phase 1
      - provider_id: vllm-inference
        provider_type: remote::vllm
        config:
          api_token: ${env.VLLM_API_TOKEN:=fake}
          max_tokens: ${env.VLLM_MAX_TOKENS:=4096}
          tls_verify: ${env.VLLM_TLS_VERIFY:=true}
          url: ${env.VLLM_URL}
      vector_io:
      - provider_id: milvus
        provider_type: inline::milvus
        config:
          db_path: /opt/app-root/src/.llama/distributions/rh/milvus.db
          kvstore:
            db_path: /opt/app-root/src/.llama/distributions/rh/milvus_registry.db
            namespace: null
            type: sqlite
      agents:
      - provider_id: meta-reference
        provider_type: inline::meta-reference
        config:
          persistence_store:
            db_path: /opt/app-root/src/.llama/distributions/rh/agents_store.db
            namespace: null
            type: sqlite
          responses_store:
            db_path: /opt/app-root/src/.llama/distributions/rh/responses_store.db
            type: sqlite
      eval: []
      files:
      - provider_id: meta-reference-files
        provider_type: inline::localfs
        config:
          metadata_store:
            db_path: /opt/app-root/src/.llama/distributions/rh/files_metadata.db
            type: sqlite
          storage_dir: /opt/app-root/src/.llama/distributions/rh/files
      datasetio:
      - provider_id: huggingface
        provider_type: remote::huggingface
        config:
          kvstore:
            db_path: /opt/app-root/src/.llama/distributions/rh/huggingface_datasetio.db
            namespace: null
            type: sqlite
      scoring:
      - provider_id: basic
        provider_type: inline::basic
        config: {}
      - provider_id: llm-as-judge
        provider_type: inline::llm-as-judge
        config: {}
      tool_runtime:
      - provider_id: rag-runtime
        provider_type: inline::rag-runtime
        config: {}
      - provider_id: model-context-protocol
        provider_type: remote::model-context-protocol
        config: {}
    metadata_store:
      type: sqlite
      db_path: /opt/app-root/src/.llama/distributions/rh/inference_store.db
    models:
    - provider_id: sentence-transformers
      model_id: granite-embedding-125m
      provider_model_id: ibm-granite/granite-embedding-125m-english
      model_type: embedding
      metadata:
        embedding_dimension: 768
    # Single Model: Local vLLM - same as Phase 1
    - provider_id: vllm-inference
      model_id: llama-32-3b-instruct
      model_type: llm
      metadata:
        description: "Local Llama 3.2-3B via vLLM"
        display_name: llama-32-3b-instruct
    shields: []
    vector_dbs: []
    datasets: []
    scoring_fns: []
    benchmarks: []
    tool_groups:
    # Built-in RAG tools
    - toolgroup_id: builtin::rag
      provider_id: rag-runtime
    # ============================================
    # MCP Server 1: Weather (same as Phase 1)
    # ============================================
    - toolgroup_id: mcp::weather-data
      provider_id: model-context-protocol
      mcp_endpoint:
        uri: http://weather-mongodb-mcp:8000/mcp
    # ============================================
    # MCP Server 2: HR Tools (NEW in Phase 2!)
    # ============================================
    - toolgroup_id: mcp::hr-tools
      provider_id: model-context-protocol
      mcp_endpoint:
        uri: http://hr-mcp-server:8000/mcp
    server:
      port: 8321
